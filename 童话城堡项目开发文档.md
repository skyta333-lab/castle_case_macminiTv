---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 08c32dbc448db9e5929e6250cbc63793_31c1d3d0af4a11f188ac525400dcc5b3
    ReservedCode1: stVEphQ57igjLwp278y4cCWcl/o8k1A3g5X7Jm6sXU1GxEUhzSARxtC7lQe2fjhYfWPlYEzhZieHo302Si2AgWnHlSO4IcbPUEGf8wDKFukVoj+C4pwPg5cwiqSs6I9fNrIqaIjwF4Gsp7B3IcMPddysd7oHIbmcnsMY5fDvok9AWuo6SuOsnxq9G14=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 08c32dbc448db9e5929e6250cbc63793_31c1d3d0af4a11f188ac525400dcc5b3
    ReservedCode2: stVEphQ57igjLwp278y4cCWcl/o8k1A3g5X7Jm6sXU1GxEUhzSARxtC7lQe2fjhYfWPlYEzhZieHo302Si2AgWnHlSO4IcbPUEGf8wDKFukVoj+C4pwPg5cwiqSs6I9fNrIqaIjwF4Gsp7B3IcMPddysd7oHIbmcnsMY5fDvok9AWuo6SuOsnxq9G14=
---

# 童话城堡项目开发文档

| 项目信息 | 内容 |
|---|---|
| 项目代号 | Fairytale Castle |
| 交付版本 | v1.0 |
| 文档日期 | 2026-09-13 |
| 目标引擎 | Godot 4.7（已用 4.7.2-stable 验证） |
| 建模工具 | Blender 4.2.3 LTS（bpy 程序化脚本无头流水线） |
| 坐标约定 | glTF / Godot Y-up，单位：米 |

---

## 1. 项目概述

本项目按参考截图重建了一座迪士尼童话风格城堡，全部几何由 Blender 的 `bpy` 脚本程序化生成（无手工建模），导出为 Godot 可直接导入的 GLB，并在 Godot 4.7 中搭建了一套可立即运行的开箱场景。

### 1.1 交付物清单

| 交付物 | 文件名 | 说明 |
|---|---|---|
| 模型源文件 | `castle_ref_model.blend` | Blender 工程，658 个对象、8 个分层集合，保留后续精修余地 |
| 模型交换格式 | `castle_ref_model.glb` | 二进制 glTF（Y-up），Godot / Unity / UE 通用 |
| Godot 工程 | `castle_godot/` | Godot 4.7 工程，含场景、脚本、材质、环境、自检工具 |
| 预览图 | `castle_ref_preview.png` | Blender 渲染的最终效果预览（阴天漫射光） |
| 开发文档 | 本文件 | 模型结构、工程结构、运行方式、可调参数、精修建议 |

### 1.2 技术路线

1. **建模**：`bpy` 脚本 `read_factory_settings(use_empty=True)` 清空场景 → 按分层 collection 用 primitive（cylinder / cone / cube / sphere）+ 布尔运算生成几何 → 按材质分组着色。
2. **导出**：先 `save_as_mainfile` 保存 `.blend`，再 `export_scene.gltf(export_format='GLB', export_apply=True, export_yup=True)` 导出模型。
3. **引擎接入**：Godot `--headless --import` 完成资源导入，命令行脚本自检场景与模型加载，确认零报错。

---

## 2. 模型结构说明

### 2.1 总体规格

| 指标 | 数值 |
|---|---|
| 对象总数 | 658（其中 MESH 656、LIGHT 1、CAMERA 1） |
| 网格节点数（Godot 侧） | 655 |
| 三角/多边形面数 | 17,191 |
| 顶点数 | 19,923 |
| 整体包围盒 | 230.0 × 230.0 × 88.29（X × Z 平面 × 高度，Y-up） |
| 包围盒中心高度 | Y = 43.145（底部 Y = -1.0，顶部 Y = 87.29） |
| 顶层容器 | 集合 `CastleRef`（位于 `Scene Collection` 下）与一个灯光、一个相机 |

### 2.2 分层集合（Blender Collection）

模型按职责划分为 8 个工作集合 + 1 个暂存集合，便于精修时按部件隔离编辑：

| 集合 | 直接对象数 | 承担内容 |
|---|---|---|
| `01_Terrain_Podium` | 9 | 赭红岩台基、阶梯、铁艺栏杆 |
| `02_Environment` | 102 | 草地、灌木、灯柱、环境装饰件 |
| `03_Walls_Massing` | 5 | 主体墙体、城门门楼体块 |
| `04_Towers` | 33 | 11 座塔身（中央主塔最高，左右对称递减） |
| `05_Roofs_Domes` | 14 | 蓝/灰蓝锥顶、绿色穹顶 |
| `06_Windows` | 273 | 尖券窗（浅色外框 + 深色玻璃贴面） |
| `07_Ornaments` | 212 | 金色装饰、风向标、徽记、线脚 |
| `08_Flags` | 8 | 两侧蓝色长旗 + 白色城堡徽记 |
| `99_Scratch` | 0 | 建模过程中的暂存集合（保留备用，不含内容） |

> 说明：集合为**嵌套结构**，`CastleRef` 为总容器，上述 01–08 为其子集合；因此按集合统计的对象数之和（656）等于全部网格对象数。

### 2.3 材质清单（16 种）

以下色值为 Blender 中的**线性空间** Base Color（导出至 glTF 后按 PBR 标准传递），Godot 侧可直接看到对应效果。

| 材质名 | 引用对象数 | Base Color (R,G,B) | Metallic | Roughness | 用途 |
|---|---|---|---|---|---|
| `Castle_Limestone` | 348 | 0.880, 0.831, 0.753 | 0.00 | 0.84 | 主墙、塔身浅色石材 |
| `Castle_WindowGlass` | 91 | 0.014, 0.019, 0.037 | 0.00 | 0.16 | 窗玻璃（深色低粗糙） |
| `Castle_Gold` | 57 | 0.597, 0.371, 0.107 | 0.75 | 0.30 | 金色装饰、风向标、门环 |
| `Castle_BronzeIron` | 54 | 0.107, 0.069, 0.042 | 0.60 | 0.55 | 铁艺栏杆、金属构件 |
| `Castle_Shrub` | 49 | 0.069, 0.133, 0.048 | 0.00 | 0.92 | 灌木植被 |
| `Castle_TerracottaBrick` | 13 | 0.672, 0.366, 0.323 | 0.00 | 0.90 | 粉砖墙体 |
| `Castle_TerracottaBrickDeep` | 12 | 0.552, 0.271, 0.246 | 0.00 | 0.92 | 粉砖暗部 / 阴影砖 |
| `Castle_DomeGreen` | 6 | 0.093, 0.209, 0.117 | 0.22 | 0.34 | 绿色穹顶 |
| `Castle_OchreRockDeep` | 6 | 0.209, 0.058, 0.037 | 0.00 | 0.95 | 台基深色岩 |
| `Castle_OchreRock` | 5 | 0.305, 0.089, 0.055 | 0.00 | 0.95 | 台基岩体 |
| `Castle_RoofDeepBlue` | 5 | 0.025, 0.042, 0.138 | 0.06 | 0.34 | 深蓝锥顶 |
| `Castle_RoofSlateBlue` | 3 | 0.084, 0.117, 0.209 | 0.06 | 0.36 | 灰蓝锥顶 |
| `Castle_FlagBlue` | 2 | 0.027, 0.072, 0.271 | 0.00 | 0.72 | 旗帜布面 |
| `Castle_FlagEmblem` | 2 | 0.871, 0.871, 0.855 | 0.00 | 0.70 | 旗帜徽记 |
| `Castle_LampGlass` | 2 | 1.000, 0.807, 0.434 | 0.00 | 0.10 | 灯罩玻璃（暖光） |
| `Castle_Meadow` | 1 | 0.122, 0.195, 0.072 | 0.00 | 0.95 | 模型自带草地底盘 |

### 2.4 关键建模约定（复现 / 二次开发须知）

- **布尔挖洞**：城门双拱门使用 `EXACT` 求解器，cutter 对象在 `modifier_apply` 后立即删除，避免残留进导出文件。
- **非均匀缩放**：扁锥屋顶（如 `scale=(1.05, 0.66, 1.0)`）在生成时即执行 `transform_apply(scale=True)`，随后才调用 `shade_auto_smooth`，否则法线与比例异常。
- **窗户贴面**：窗不用真布尔挖洞，而以"浅色外框盒 + 更靠外的深色玻璃盒"贴面模拟，控制面数与失败风险。
- **旗帜**：`from_pydata` 构建单面 mesh 后加 `SOLIDIFY` 修改器赋予厚度。

---

## 3. Godot 工程结构与场景说明

### 3.1 目录结构

```
castle_godot/
├── project.godot                      # 工程配置（主场景、渲染、窗口）
├── icon.svg                           # 工程图标
├── assets/
│   └── models/
│       ├── castle_ref_model.glb       # 城堡模型（PackedScene 导入）
│       └── castle_ref_model.glb.import# 导入配置与资源 UID
├── materials/
│   ├── env_day.tres                   # 天空 + 环境（ProceduralSky / 环境光 / 雾 / 泛光）
│   └── ground_grass.tres              # 地面草地材质
├── scenes/
│   └── main.tscn                      # 主场景（运行入口）
├── scripts/
│   └── castle_showcase.gd             # 展示场景根脚本（相机环绕 / 参数暴露）
└── tools/
    └── verify_project.gd              # 命令行自检脚本（SceneTree 脚本）
```

### 3.2 `project.godot` 关键配置

| 配置项 | 值 | 说明 |
|---|---|---|
| `run/main_scene` | `res://scenes/main.tscn` | 运行入口场景 |
| `config/features` | `4.7`, `Forward Plus` | 目标版本与渲染后端 |
| 视口分辨率 | 1600 × 900 | 窗口默认尺寸 |
| `stretch/mode` | `canvas_items` | UI 缩放模式 |
| `anti_aliasing/quality/msaa_3d` | 2（4× MSAA） | 3D 抗锯齿 |
| `default_clear_color` | (0.72, 0.82, 0.90) | 缺省天空底色 |
| 方向光软阴影质量 | 3 | 柔化阴影边缘 |

### 3.3 主场景节点树（`scenes/main.tscn`）

| 节点 | 类型 | 关键配置 |
|---|---|---|
| `CastleShowcase` | `Node3D` | 挂载 `castle_showcase.gd`，场景根 |
| `WorldEnvironment` | `WorldEnvironment` | 引用 `res://materials/env_day.tres`（天空 + 环境光 + 雾 + 泛光） |
| `SunLight` | `DirectionalLight3D` | 暖白 (1, 0.957, 0.878)，能量 1.35，阴影开启，阴影最远 320m |
| `Ground` | `MeshInstance3D` | `PlaneMesh` 600 × 600，Y = −1.02（略低于台基底面，避免 Z-fighting），覆盖草地材质 |
| `Castle` | GLB 实例 | `castle_ref_model.glb` 的场景实例，位于原点 |
| `MainCamera` | `Camera3D` | 初始位姿 (149.1, 95, 212.98) 朝向 (0, 38, 0)，FOV 45°，`current = true` |

> 城堡实例的世界包围盒经自检确认为 230.0 × 88.29 × 230.0、中心 (0, 43.145, 0)，与 Blender 源模型完全一致，说明 GLB 导入无缩放/朝向偏差。

### 3.4 展示脚本 `scripts/castle_showcase.gd`

职责：初始化相机环绕、对齐相机朝向、输出场景自检信息（模型世界包围盒）。

- 相机在 `_process` 中按 `camera_orbit_speed` 绕 Y 轴环绕，始终 `look_at` 注视点。
- 可选的太阳环绕（`sun_orbit_speed`）。
- 提供 `_world_aabb()` / `_collect_visuals()` 工具函数，递归合并 `VisualInstance3D` 的世界空间 AABB，供自检与二次开发复用。

### 3.5 材质与环境资源

- `materials/env_day.tres`：`ProceduralSkyMaterial`（天顶蓝 / 地平线浅蓝 / 地面绿）+ `Sky`（`process_mode = 2` 实时更新，`radiance_size = 4`）+ `Environment`（`ambient_light_source = Sky`、Filmic 色调映射、SSAO 半径 2.0、泛光强度 0.5、雾密度 0.0008）。
- `materials/ground_grass.tres`：`StandardMaterial3D`，草地绿 albedo (0.239, 0.361, 0.176)、roughness 0.95。
- 城堡本体材质来自 GLB 内嵌 PBR（见 §2.3 的 16 种材质），**未做覆盖**，如需替换可在 `Castle` 实例上使用 `material_override` 或在导入面板的材质重映射中指定。

### 3.6 自检脚本 `tools/verify_project.gd`

一个 `SceneTree` 脚本，无需图形界面即可验证工程健康度，检查项包括：

1. 关键文件存在性（工程配置、主场景、模型、两个 `.tres`、展示脚本）；
2. 模型可加载为 `PackedScene` 且网格节点数 > 0、世界包围盒高度 > 0；
3. 主场景可加载为 `PackedScene`，且 `WorldEnvironment` / `SunLight` / `Ground` / `Castle` / `MainCamera` 五类节点类型正确；
4. 主相机 `current` 为真、`WorldEnvironment` 已挂载 `Environment` 资源；
5. 主场景挂载到 `SceneTree` 并稳定运行 2 帧。

退出码 0 表示全部通过，非 0 表示存在失败项（可直接接入 CI）。

---

## 4. 导入与运行步骤

### 4.1 前置条件

| 依赖 | 版本 | 说明 |
|---|---|---|
| Godot Engine | 4.7.x（验证版本 4.7.2-stable） | 需 Standard 版，无需 .NET |
| 操作系统 | macOS / Windows / Linux | 文档命令以 macOS 为例 |

Godot 可执行文件（本例）：`/Applications/Godot.app/Contents/MacOS/Godot`

### 4.2 图形界面方式（日常开发）

1. 启动 Godot，选择 **导入（Import）**；
2. 浏览到 `castle_godot/project.godot`，点击 **Import & Edit**；
3. 首次打开时引擎会自动重建 `.godot/` 导入缓存（约数秒）；
4. 按 **F5** 运行，即可看到城堡场景与缓慢环绕的相机。

### 4.3 命令行方式（无界面 / CI）

```bash
GODOT="/Applications/Godot.app/Contents/MacOS/Godot"
PROJ="/path/to/castle_godot"

# 1) 导入 / 刷新资源（首次或改动资源后执行）
"$GODOT" --headless --path "$PROJ" --import

# 2) 运行主场景（--quit-after 指定运行帧数后自动退出）
"$GODOT" --headless --path "$PROJ" --quit-after 300

# 3) 工程自检（退出码 0 = 全部通过）
"$GODOT" --headless --path "$PROJ" --script res://tools/verify_project.gd
```

> 迁移到新机器时，`.godot/` 缓存可删除，执行第 1 步即可重新生成；`.import` 文件需随工程一起保留。

### 4.4 本次交付的验证记录

| 验证项 | 命令 | 结果 |
|---|---|---|
| 资源导入 | `--headless --path . --import` | 成功，`castle_ref_model.glb` 导入为 `PackedScene`（UID `uid://ds3ghqfy6ffyh`） |
| 主场景运行 | `--headless --path . --quit-after 300` | 退出码 0，无任何 ERROR/WARNING 输出 |
| 工程自检 | `--script res://tools/verify_project.gd` | 17 项检查全部通过（`RESULT: ALL CHECKS PASSED`），退出码 0 |
| 模型尺寸一致性 | 自检脚本内 AABB 比对 | Godot 侧 230.0 × 88.29 × 230.0，与 Blender 源一致 |

自检脚本实际输出（节选）：

```
[OK  ] 模型加载为 PackedScene
[OK  ] 模型网格节点数 655 (>0)
[OK  ] 模型世界包围盒有效 (高度 88.29 > 0)
    模型世界包围盒尺寸: (230.0, 88.29, 230.0) , 中心: (0.0, 43.145, 0.0)
[castle_godot] scene ready | castle loaded: true
检查项总数: 17 , 失败: 0
RESULT: ALL CHECKS PASSED
```

---

## 5. 可调参数

### 5.1 相机参数（`CastleShowcase` 节点检查器 / `castle_showcase.gd`）

| 参数 | 默认值 | 说明 |
|---|---|---|
| `camera_distance` | 260.0 | 相机到场景中心的水平距离（整体城堡建议 200–350） |
| `camera_height` | 95.0 | 相机高度（Y）。降低可获得更"仰视"的宏伟感 |
| `camera_focus_height` | 38.0 | 注视点高度，城堡主体大致位于 0–88 |
| `camera_orbit_speed` | 6.0 | 环绕速度（度/秒），设为 0 即静止 |
| `camera_start_angle` | 35.0 | 初始方位角（度） |

### 5.2 光照参数

| 参数 | 位置 | 默认值 | 说明 |
|---|---|---|---|
| `sun_orbit_speed` | `castle_showcase.gd` | 0.0 | 阳光绕 Y 轴旋转速度（度/秒），0 为静止 |
| `light_energy` | `main.tscn` → `SunLight` | 1.35 | 阳光强度，阴天感可降至 0.9–1.1 |
| `light_color` | `main.tscn` → `SunLight` | (1, 0.957, 0.878) | 暖白日光色 |
| `shadow_enabled` / `directional_shadow_max_distance` | `main.tscn` → `SunLight` | true / 320.0 | 阴影开关与阴影覆盖距离 |
| `shadow_bias` / `shadow_normal_bias` | `main.tscn` → `SunLight` | 0.03 / 1.5 | 出现阴影痤疮 / 漏光时微调 |

### 5.3 环境与材质参数（`materials/env_day.tres`、`materials/ground_grass.tres`）

| 参数 | 默认值 | 说明 |
|---|---|---|
| `sky_top_color` / `sky_horizon_color` | (0.255, 0.49, 0.804) / (0.749, 0.847, 0.941) | 天空渐变两端颜色，可做黄昏 / 夜景 |
| `sun_angle_max` / `sun_curve` | 30.0 / 0.15 | 太阳盘面大小与地平线暖光扩散 |
| `ambient_light_energy` | 1.0 | 环境光强度（来源为天空） |
| `tonemap_mode` / `tonemap_white` | Filmic / 6.0 | 色调映射方式与白点 |
| `ssao_enabled` / `ssao_intensity` | true / 1.6 | 环境光遮蔽，增强结构层次 |
| `glow_enabled` / `glow_intensity` | true / 0.5 | 泛光，突出金色与灯罩高光 |
| `fog_enabled` / `fog_density` | true / 0.0008 | 大气雾，营造空气透视 |
| `ground_grass.tres` → `albedo_color` / `roughness` | (0.239, 0.361, 0.176) / 0.95 | 地面颜色与粗糙度 |

> 所有 `@export` 参数均可在编辑器检查器中实时调整，无需改代码。

---

## 6. 后续精修建议

### 6.1 视觉表现

1. **材质升级**：为 `Castle_Limestone`、`Castle_TerracottaBrick` 增加 normal / roughness 贴图与轻微色相变化，打破纯色面感；金色件可加金属度贴图与边缘磨损。
2. **窗外发光**：给 `Castle_WindowGlass` 增加自发光（`emission`），配合夜晚天空即可得到"灯火通明"的童话夜景。
3. **环境叙事**：补几组飞鸟、飘动的旗面（顶点动画或 `AnimationPlayer` 正弦摆动）、护城河水面（`StandardMaterial3D` 的折射 + 波纹法线）。
4. **后处理**：适度提高 `glow_intensity` 与增加 `adjustment` 饱和度，做出迪士尼式的暖色调。

### 6.2 性能

1. 当前 17K 面、655 个网格节点，节点数偏多（几何碎片化）。如需用于移动端，建议在 Blender 中按材质合并同层对象（`join`），可将网格节点数降至 20 以内，显著降低 draw call。
2. 远处的 `06_Windows`（273 个对象）可生成 LOD1：以贴图纹理替代实体窗框。
3. 若场景需要大量重复塔楼，可将部件转为 `MultiMeshInstance3D` 或复用 `MeshInstance3D` 共享资源。

### 6.3 工程化

1. 将 `tools/verify_project.gd` 接到 CI（如 GitHub Actions 无头跑 Godot），每次改动资源后自动校验。
2. 若需二次编辑模型，保留 `castle_ref_model.blend` 的分层集合结构，按 `01`–`08` 逐层修改后重新导出 GLB 覆盖 `assets/models/`，工程侧无需改动。
3. 可把相机环绕参数抽到 `resource`（`Resource` 类）中，供多套"运镜预设"复用（巡览 / 特写 / 俯拍）。

---

*文档结束。如需调整模型结构、增加部件或变更渲染风格，请以 `castle_ref_model.blend` 为唯一几何源，并同步重新导出 GLB。*
*（内容由AI生成，仅供参考）*
